// Shared constants for the read-only public demo. The "View Demo" button on the
// landing page auto-signs-in this account (by password — no email/inbox needed),
// dropping visitors straight into a fully-seeded, read-only org.
export const DEMO_EMAIL = "demo@xeltrixchem.com";

// Fixed UUID for the demo org so code + seed script + cookie all agree.
export const DEMO_TEAM_ID = "00000000-0000-0000-0000-0000000d3110";

export const DEMO_TEAM_NAME = "Xeltrix Team — Demo";

// Synthetic email domain for the demo's fake staff members.
export const DEMO_STAFF_DOMAIN = "demo.local";
